{
  "name": "helpxs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "echo 'Run dev:web and dev:api in two terminals'",
    "dev:web": "npm run dev -w web",
    "dev:api": "npm run dev -w api",
    "build": "npm run build -w web && npm run build -w api",
    "build:web": "npm run build -w web",
    "deploy": "npm run build -w web && npm run deploy -w api",
    "typecheck": "npm run typecheck -w web --if-present && npm run typecheck -w api --if-present"
  }
}
